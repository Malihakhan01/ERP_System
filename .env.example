# .env.example
# Copy this file to .env.local and fill in your Database project credentials.
# NEVER commit .env.local to version control.

# ----- Database (required for auth + database in Phase 2+) -----

# SERVICE ROLE KEY — NEVER expose to browser. Server-side only.

# ----- App -----
NEXT_PUBLIC_APP_NAME=FactoryOS
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ----- AI (Phase 3+) — server-side only, never expose to browser -----
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GOOGLE_AI_API_KEY=
